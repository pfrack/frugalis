services:
  - type: web
    name: cerebrum
    runtime: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/cerebrum
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: info